---
title: "Averages and range"
lede: "Mean, median, mode and range, and which average suits which situation."
---

<LearningObjectives>
  <li>Define and explain Averages and range in your own words</li>
  <li>Use key terms such as Median accurately</li>
  <li>Apply what you have learned to new examples and questions</li>
  <li>Avoid the common mistakes learners make with this topic</li>
</LearningObjectives>

Statistics turns raw data into decisions — but only when it is done honestly. These lessons teach you to summarise data with averages, display it clearly, and spot when a graph is trying to fool you.

This lesson focuses on Averages and range: mean, median, mode and range, and which average suits which situation.

<Definition term="Averages and range">
  Mean, median, mode and range, and which average suits which situation.
</Definition>

## Key ideas

### Choosing an average

Use the mean for symmetric data with no extremes, the median when a few extreme values would distort the mean (such as salaries), and the mode for categories (such as favourite colours). Always consider the range too, since identical averages can hide very different spreads.

### Correlation is not causation

A strong correlation only shows that two things vary together; a third factor may cause both. A line of best fit drawn through the middle of the points shows the trend and can estimate values within the data range, but never extrapolate far beyond it.

**Key term — Median:** The middle value when data is ordered from smallest to largest; a robust average unaffected by extreme values.

<WorkedExample title="Summarising a data set">
  Find the mean, median, mode and range of: 3, 5, 5, 8, 9.

  Mean: (3 + 5 + 5 + 8 + 9) ÷ 5 = 30 ÷ 5 = 6.
  Median: the data is ordered; the 3rd of 5 values is 5.
  Mode: 5 appears most often (twice).
  Range: 9 − 3 = 6.

  **Answer:** Mean 6, median 5, mode 5, range 6.
</WorkedExample>

<ImportantNote title="Common mistakes">
  - **Finding the median without ordering the data** Always sort the values first; the median is the middle of the ordered list, not the middle of the original list.
  - **Forgetting that an even count has no single middle value** With an even number of values, the median is halfway between the two middle values, e.g. (6 + 8) ÷ 2 = 7.
</ImportantNote>

## Practice

<PracticeQuestions>
  <PracticeItem question="Six test scores are 5, 6, 7, 8, 9 and 40. Which average best represents a typical score?" hint="One value is extreme.">
    The median (7.5); the mean (12.5) is distorted by the outlier 40.
  </PracticeItem>
  <PracticeItem question="Find the mode and range of 2, 6, 4, 6, 6, 3." hint="Mode is most frequent; range is max minus min.">
    Mode = 6; range = 6 − 2 = 4.
  </PracticeItem>
  <PracticeItem question="Find the median of 11, 4, 9, 2, 7." hint="Order the data first.">
    Ordered: 2, 4, 7, 9, 11; median = 7.
  </PracticeItem>
  <PracticeItem question="Find the mean of 4, 7, 9, 12, 18." hint="Add, then divide by 5.">
    Sum = 50; mean = 50 ÷ 5 = 10.
  </PracticeItem>
</PracticeQuestions>

## Quick check

<Quiz title="Averages and range — quick check">
  <QuizQuestion question="Which of these best defines &quot;Median&quot;?" options={["The difference between the largest and smallest values, giving a simple measure of how spread out the data is.", "A pattern in a scatter diagram where two variables move together — positive, negative or none — which does not by itself prove causation.", "The total of all values divided by how many values there are; the most common average, but sensitive to extreme values.", "The middle value when data is ordered from smallest to largest; a robust average unaffected by extreme values."]} answer={3} explanation="The middle value when data is ordered from smallest to largest; a robust average unaffected by extreme values." />
  <QuizQuestion question="Why might a bar chart with a y-axis starting at 50 be misleading?" options={["The total of all values divided by how many values there are; the most common average, but sensitive to extreme values.", "Always sort the values first; the median is the middle of the ordered list, not the middle of the original list.", "Finding the median without ordering the data", "Bars look far more different than the values are, because the truncated axis exaggerates small differences."]} answer={3} explanation="Bars look far more different than the values are, because the truncated axis exaggerates small differences." />
  <QuizQuestion question="A scatter diagram shows points rising left to right. What type of correlation is this?" options={["The total of all values divided by how many values there are; the most common average, but sensitive to extreme values.", "Always sort the values first; the median is the middle of the ordered list, not the middle of the original list.", "Finding the median without ordering the data", "Positive correlation."]} answer={3} explanation="Positive correlation." />
</Quiz>

<Summary>
  - Averages and range: mean, median, mode and range, and which average suits which situation.
  - Choosing an average: Use the mean for symmetric data with no extremes, the median when a few extreme values would distort the mean (such as salaries), and the mode for categories (such as favourite colours).
  - Mean: The total of all values divided by how many values there are; the most common average, but sensitive to extreme values.
  - Watch out for: finding the median without ordering the data
</Summary>
